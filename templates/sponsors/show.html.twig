{% include 'baseBack.html.twig'%}
<!DOCTYPE html>

<title>Sponsors</title>

{% block content1 %}
<div class="row" style="margin-left:20%">
    <h1>Sponsors</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sponsor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sponsor.name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ sponsor.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sponsors_index') }}">back to list</a>

    <a href="{{ path('sponsors_edit', {'id': sponsor.id}) }}">edit</a>

    {{ include('sponsors/_delete_form.html.twig') }}

    </div>
{% endblock %}
