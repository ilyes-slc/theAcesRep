{% include 'amal/baseBack.html.twig'%}

{% block content1 %}

<div class="row" style="margin-left:20%">


<title>Tournoi</title>

    <h1>Tournoi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tournoi.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tournoi.name }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ tournoi.dateDebut ? tournoi.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ tournoi.dateFin ? tournoi.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ tournoi.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ tournoi.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tournoi_index') }}">back to list</a>

    <a href="{{ path('tournoi_edit', {'id': tournoi.id}) }}">edit</a>

    {{ include('amal/tournoi/_delete_form.html.twig') }}
    </div>
{% endblock %}
